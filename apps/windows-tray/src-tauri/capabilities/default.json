{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the Pibo local ASR tray app.",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default"
  ]
}
